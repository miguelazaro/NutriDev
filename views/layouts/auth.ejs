<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= title || 'Acceso - NutriDev' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <!-- RUTA ABSOLUTA para Tailwind -->
  <link rel="stylesheet" href="/css/output.css">
  
  <!-- Fuente Inter -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  
  <!-- FontAwesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  
  <!-- Estilos adicionales para la imagen -->
  <style>
    .feature-image {
      width: 25rem;
      max-width: 100%;
      height: auto;
      display: block;
      margin: 0 auto;
    }
  </style>
</head>
<body class="bg-gray-100 min-h-screen p-4 flex items-center justify-center">
  <div class="w-full max-w-5xl bg-white rounded-xl shadow-lg overflow-hidden flex flex-col md:flex-row">
    <!-- Columna izquierda (gradiente verde) -->
    <div class="hidden md:block md:w-1/2 bg-gradient-to-br from-emerald-400 to-emerald-600 p-10 text-white">
      <div class="flex flex-col h-full justify-between">
        <!-- Logo arriba con fondo verde -->
        <div class="mb-8">
          <img src="/assets/img/imagen_nutridev1.png" alt="NutriDev" class="feature-image">
        </div>
        
        <!-- Contenido informativo -->
        <div class="mt-8">
          <h1 class="text-3xl font-bold mb-4">Haz crecer tu negocio y mejora tu relación con los clientes</h1>
          <p class="text-lg mb-8">Organiza tus planes alimenticios en una sola plataforma diseñada para nutriólogos modernos como tú.</p>
          
          <ul class="space-y-3">
            <li class="flex items-center">
              <i class="fas fa-check-circle mr-3 text-white"></i>
              <span>Creación de dietas y recetas</span>
            </li>
            <li class="flex items-center">
              <i class="fas fa-check-circle mr-3 text-white"></i>
              <span>Gestión de clientes</span>
            </li>
            <li class="flex items-center">
              <i class="fas fa-check-circle mr-3 text-white"></i>
              <span>Seguimiento del progreso</span>
            </li>
          </ul>
        </div>
        
        <!-- Pie de página con los enlaces (como en la imagen 2) -->
        <div class="text-sm text-white text-opacity-80 mt-8 pt-6 border-t border-white border-opacity-20">
          <div class="flex flex-wrap gap-4">
            <a href="#" class="hover:underline">Configuración de cookies</a>
            <a href="#" class="hover:underline">Política de Privacidad</a>
            <a href="#" class="hover:underline">Términos y Condiciones</a>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Columna derecha (formulario) -->
    <div class="w-full md:w-1/2 p-8 md:p-10">
      <!-- Logo para móviles -->
      <div class="md:hidden text-center mb-6">
        <img src="/assets/img/logo_nutridev.png" alt="NutriDev" class="w-40 mx-auto">
      </div>
      
      <!-- Contenido dinámico -->
      <%- body %>
    </div>
  </div>
</body>
</html>