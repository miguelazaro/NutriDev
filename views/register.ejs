<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Registro - NutriDev</title>
  <link rel="stylesheet" href="/css/output.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
  <form method="POST" action="/register" class="bg-white p-8 rounded shadow-md w-full max-w-sm">
    <h1 class="text-2xl font-bold mb-6 text-center">Crear cuenta</h1>
    <% if (error.length > 0) { %>
      <div class="text-red-600 mb-4 text-sm"><%= error[0] %></div>
    <% } %>
    <input name="nombre" type="text" placeholder="Nombre completo" required class="w-full p-2 mb-4 border rounded" />
    <input name="email" type="email" placeholder="Correo electrónico" required class="w-full p-2 mb-4 border rounded" />
    <input name="password" type="password" placeholder="Contraseña" required class="w-full p-2 mb-4 border rounded" />
    <button type="submit" class="bg-emerald-500 text-white w-full p-2 rounded hover:bg-emerald-600">Registrarme</button>
    <p class="mt-4 text-sm text-center">¿Ya tienes cuenta? <a href="/login" class="text-blue-600 underline">Inicia sesión</a></p>
  </form>
</body>
</html>
