<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        <%= paciente ? 'Editar' : 'Nuevo' %> Paciente | NutriDev
    </title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
</head>

<body class="bg-gray-50 min-h-screen font-sans text-gray-800">

    <!-- Breadcrumb -->
    <div class="px-6 py-4 text-sm text-gray-500 text-right">
        <a href="/" class="hover:underline text-emerald-600">Inicio</a> /
        <a href="/pacientes" class="hover:underline text-emerald-600">Pacientes</a> /
        <span>
            <%= paciente ? 'Editar' : 'Nuevo' %>
        </span>
    </div>

    <main class="max-w-3xl mx-auto px-6 py-8 bg-white rounded-xl shadow-md">
        <h1 class="text-2xl font-bold mb-6 text-gray-800">
            <%= paciente ? 'Editar' : 'Nuevo' %> Paciente
        </h1>

        <form method="POST" action="<%= paciente ? '/pacientes/actualizar/' + paciente.id : '/pacientes/guardar' %>"
            class="space-y-6">

            <!-- Nombre y Edad -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                    <label class="block text-sm font-medium mb-1">Nombre:</label>
                    <input type="text" name="nombre" value="<%= paciente ? paciente.nombre : '' %>" required
                        class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:border-transparent"
                        placeholder="Nombre del paciente" />
                </div>
                <div>
                    <label class="block text-sm font-medium mb-1">Edad:</label>
                    <input type="number" name="edad" value="<%= paciente ? paciente.edad : '' %>" required
                        class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:border-transparent"
                        placeholder="Edad" />
                </div>
            </div>

            <!-- Historial Médico -->
            <div>
                <label class="block text-sm font-medium mb-1">Historial Médico:</label>
                <input type="text" name="historial" value="<%= paciente ? paciente.historial : '' %>"
                    class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:border-transparent"
                    placeholder="Alergias, condiciones, etc." />
            </div>

            <!-- Botón -->
            <div>
                <button type="submit"
                    class="inline-flex items-center gap-2 bg-gradient-to-br from-emerald-500 to-emerald-700 text-white font-semibold px-6 py-3 rounded-xl shadow-lg hover:from-emerald-600 hover:to-emerald-800 transition-transform transform hover:scale-105">
                    <i class="fas fa-save"></i>
                    <%= paciente ? 'Actualizar' : 'Guardar' %>
                </button>
            </div>
        </form>
    </main>
</body>

</html>